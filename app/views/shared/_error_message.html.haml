- if entity.errors.any?
  %section#errors
    %h4= "#{entity.errors.count} #{entity.errors.count > 1 ? "errors" : "error"} prohibited this link from being shortened"
    %ul
      - entity.errors.full_messages.each do |msg|
        %li= msg
